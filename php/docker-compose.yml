version: '3'
services:
    php:
        build:
            context: ./
            dockerfile: Dockerfile
        container_name: php-server
        restart: always
        ports:
         - "8080:80"
        volumes: 
            - ./www:/var/www/html

volumes:
    www: {}           
